## 2024-05-30 - Prevent memory allocation in animation loop
**Learning:** `new Uint8Array(analyser.frequencyBinCount)` is being instantiated inside the `drawVis` loop running via `requestAnimationFrame`. This causes unnecessary garbage collection pressure and could stutter the animation. Wait, I saw in memory that `visDataArray` is ALREADY declared as a global variable. Let me check if the codebase currently has `new Uint8Array` inside the loop.
## 2024-05-30 - Cancel `requestAnimationFrame` for hidden elements
**Learning:** `requestAnimationFrame` will continue to run even if an element is hidden via `display: none`. This is specifically happening with `#starCanvas` which is `display: none` by default but its animation loop starts anyway via `drawStars()` and loops continuously. This creates unnecessary CPU/GPU burn for a canvas that isn't visible, unless the `visible` class is toggled (which it seems it does but then again maybe we should be explicit with IntersectionObserver or similar, or just not run it if `display: none`). Let me actually implement hoisting the array allocation for `drawVis` and/or preventing `drawStars` from rendering if it is hidden or offscreen.
## 2024-05-30 - Prevent memory allocation in animation loop
**Learning:** `new Uint8Array(analyser.frequencyBinCount)` is being instantiated inside the `drawVis` loop running via `requestAnimationFrame`. This causes unnecessary garbage collection pressure and could stutter the animation. Wait, I saw in memory that `visDataArray` is ALREADY declared as a global variable. No wait, it seems it is NOT currently declared as a global variable in `index.html`. It says "Not found". The memory hint says: "To prevent garbage collection pressure, the drawVis function in index.html caches and reuses a single Uint8Array (visDataArray) rather than instantiating a new one every frame." and "In index.html, the visDataArray is declared as a global variable, initialized in startAudioVis using analyser.frequencyBinCount, and reused in drawVis with a length-check guard to ensure the buffer matches the current analyser state."  Wait, currently I searched for `let visDataArray` and it returned nothing! This means the memory is actually a hint of what TO DO, or I missed it. Wait! The memory says "To prevent garbage collection pressure..." maybe it was a past PR that got reverted or I need to implement it! Yes! The file clearly shows `const data = new Uint8Array(analyser.frequencyBinCount);` inside `drawVis`!
